# 安全说明

当前版本是供本地虚构数据演示和开发使用的 MVP 预发布版，暂不建议承载真实业务数据或直接用于生产部署。安全修复以最新主分支和最新预发布版本为对象，历史版本暂无单独维护承诺。

## 报告漏洞

请勿在公开 issue、PR 或评论中粘贴真实数据、凭据或可直接利用的漏洞细节。

仓库启用 GitHub 私密漏洞报告后，可在 **Security → Report a vulnerability** 提交报告；若入口不可见，可以先开一个仅请求私密联系渠道的 issue，不附漏洞细节。

报告尽量包含受影响版本、虚构数据的最小复现、预期与实际行为、影响范围。不要提交访问令牌、原始业务导出或生产日志。维护者核实后会讨论修复和披露时间，目前不承诺固定响应时限。

## 当前部署边界

- 本地演示账户及其统一口令是公开测试资料；Compose 的数据库免密配置只用于绑定本机的演示库。
- 登录失败保护使用单机内存，重启清零，多实例不共享；默认不信任转发头。公网入口限流和可信代理需要单独配置与验证。
- 导入按租户、媒体、日期整体替换数据，使用进程内后台任务，尚无进程中断后的持久恢复。
- Python 参考服务保留未实现操作；当前业务入口为 Java 服务。

任何公开部署都应先完成 [发布清单](docs/release-checklist.md) 中的账号、密钥、网络入口与权限检查。开源源代码不等于这些部署条件已经满足。
