# v1 本机演示验收

日期：2026-09-17。版本：`1.0.0-rc.1`。入口：`http://127.0.0.1:5173`，Java：本机 `127.0.0.1:8080`。本记录保留开源前的本机验收证据。维护者随后于同日确认开源；最新发布与远端 CI 状态见 [STATUS](../docs/STATUS.md)。公网部署未执行。

## 四项收尾

| 项目 | 完成结果 |
|---|---|
| 权限缺陷 | F-01 空授权集修复；补齐账号管理产品/运营目标范围。修改前失败、修改后同尺通过，见 [专项记录](f01-permission-regression.md)。 |
| 演示范围 | 10 个真实页面，mock 拦截停用；无权限及旧原型地址不挂载页面。 |
| 当前运行入口 | Java 已换成构建通过的 rc.1 JAR，health 返回该版本，前端运行最新主分支；浏览器完整走通以下业务路径。 |
| 交付材料 | README、演示指南、产品范围、权限矩阵、契约及类型、发布清单和虚构样表已同步；本地候选标签在收口提交后建立。 |

## 实际浏览器与文件回执

| 操作 | 观察结果 |
|---|---|
| 五个虚构账号登录 | admin、company、operator、agency、readonly 均成功；首页为聚合看盘。 |
| 管理员默认近 7 天查询 | 2026-09-10 至 09-16，9 个产品分组，消耗 1,143,901.66、收益 1,205,265.96、ROI 105.4%；表格合计一致。种子截至 09-15，因此最后一天无种子数据。 |
| 管理员 CSV | 页面点击导出后实际产生 `iaa-aggregate-2026-09-10_2026-09-16.csv`，读取文件确认 9 行，消耗/收益合计与页面一致。SHA256：`469F18608FEB2D5F9423E4DFFFECBAB8534EEAAB6F5C6EF74F7AB3487DF81E2B`。 |
| 第一次 Excel 上传 | vivo，2020-01-01/02；已完成，写入 3、替换 0；第 5 行“小时超出 0-23：99”，仅坏行跳过。 |
| 同文件重复上传 | 已完成，写入 3、替换 3；再次查库仍 3 行、消耗 600、收益 560，没有累加。两次任务 finished_at 均不早于 created_at。 |
| 页面回看样例 | 修改日期后原始明细显示 3 行；切到聚合沿用日期，显示消耗 600、收益 560、ROI 93.3%，合计一致。随后恢复近 7 天。 |
| 审计 | 页面显示两次 demo.admin 上传记录，包含媒体和文件名。 |
| 代理隔离 | 菜单 4 页；代理筛选只有“星河代理”；消耗 325,837.72，无收益/ROI。直接访问 `/analysis/trend` 显示无权访问，不加载报表。 |
| 只读隔离 | 菜单只有聚合、分天、指标说明 3 页；直接访问 `/system/imports` 被阻止。 |
| 运营与图表 | 无系统写管理入口；显示其范围内消耗 164,899.46、收益 165,042.97，趋势曲线实际渲染。 |
| 管理页面 | 用户与权限显示 5 个演示账号；账户分配显示 33 个已有归属。此轮未改演示账号权限或账户映射。 |
| 原型地址 | 管理员访问 `/products` 也显示页面不可用；不存在 mock 成功提示。 |

自动化工具的 download 事件等待超时，但下载目录中的真实文件已核对；没有重按按钮或重复导出。日期输入通过原生日期控件设置并确认“有未生效的修改”后提交，不将只改变 DOM 值当作查询完成。

## 数据影响

原有 51,840 行种子数据保留不变。导入前已确认上述 2020 年两天无 vivo 数据；本轮保留 3 行虚构样例供维护者复演，当前共 51,843 行。未重跑种子，未批量清库。两条上传任务和审计记录保留。

## 自动检查

- Java `gradle build`：98 项、0 失败、0 错误，含原有 78 项和新授权/管理范围 20 项。
- Python：45 项通过，2 条依赖弃用警告；专用测试库由夹具创建和移除。
- 前端：TypeScript 与 Vite 构建通过，保留既有大分包警告。
- 当前两服务 parity：57 项、0 失败、0 契约错误、17 处已解释差异。
- 契约导出与类型再生一致；补齐开发/测试/生产环境元数据，后两者明确为未部署占位。该修正后契约专项 15 项通过。
- dev-plm 全量检查 `ok: true`；只记录本机回执。

本机验收时 GitHub Actions 尚未运行；后续远端结果单独记录在 [STATUS](../docs/STATUS.md)。源码公开与公网部署分开验收；公网密钥、只读账号、可信代理和入口限流仍按 [发布清单](../docs/release-checklist.md) 执行。

## 最终入口复核

收口检查发现既有 Vite 进程已退出，5173 请求被拒绝。按本次 demo 授权用既有 Node/Vite 恢复为后台本机进程，未安装依赖；随后入口与代理 health 均成功，浏览器重新加载回到管理员聚合页。

另观察到 8080 已由主工作区的 bootRun 进程接管（非本次启动的 JAR 进程，当前监听为 `::`）。未干预该项目进程；核对其 `FactQueryRepository`、`UserAdminService`、`LoginProtection` 编译类与本次 98 项测试通过的构建 SHA256 全部一致，health 仍为 rc.1。因此本记录不将最终运行方式描述为仅回环监听或只运行本轮 JAR。公开部署仍未执行。
