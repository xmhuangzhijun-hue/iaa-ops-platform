# 复制为 .env 后按需修改；.env 不入库。
# 根目录 .env 仅供 Docker Compose 读取，不会自动注入 Java 或 Python 进程。
# PostgreSQL 数据目录：留空使用 Docker 命名卷，也可指向本机路径。
IAA_PG_DATA=
IAA_PG_PORT=55433
# 修改数据库端口后，请在各自启动终端分别设置 DATABASE_URL：
# Java 使用 jdbc:postgresql://127.0.0.1:<端口>/iaa_ops
# Python 使用 postgresql+psycopg://iaa@127.0.0.1:<端口>/iaa_ops
# Java 用户名另由 DATABASE_USER 设置（默认 iaa）；两种连接 URL 不能混用。
