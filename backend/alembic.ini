[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
path_separator = os
# Keep this file ASCII-only: configparser reads it with the OS locale encoding.
# The connection URL comes from DATABASE_URL via migrations/env.py.
